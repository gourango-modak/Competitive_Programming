#include<bits/stdc++.h>
using namespace std;

int findDiff(vector<int> &cho, int start, int end, long long rajib, long long rakib) {

	findDiff(cho, i

int main() {
    int n;
    cin >> n;
    vector<int> cho(n);
    for(int i = 0; i < n; i++) cin >> cho[i];

    return 0;
}